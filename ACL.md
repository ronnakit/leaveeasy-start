# 📋 ตารางกำหนดสิทธิ์การเข้าถึง (Access Control List - ACL)

> **ระบบขอลาออนไลน์ LeaveEasy**  
> **อัปเดตล่าสุด:** สัปดาห์ที่ 7 (ระบบรู้จักผู้ใช้ และออกสู่โลก)  
> **ผู้จัดทำ:** พี่เชิด (Lead Developer) & มะลิ AI

---

## 1. ตารางสรุปสิทธิ์ของแต่ละบทบาท (Permission Matrix)

| บทบาท (Role) | รหัสในระบบ | ดูใบลา | ยื่นใบลา | อนุมัติ / ไม่อนุมัติ | จัดการประเภทการลา | ลบใบลา | สิ่งที่ทำไม่ได้ (Restrictions) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **🙋 พนักงานทั่วไป (Employee)** | `employee` | เฉพาะของตัวเอง | ได้ | **ไม่ได้** | **ไม่ได้** | ได้ (เฉพาะใบของตัวเองที่สถานะ `รอพิจารณา`) | • ไม่อนุมัติหรือปฏิเสธใบลาตนเอง/ผู้อื่น<br>• ไม่สามารถดูใบลาของผู้อื่นได้<br>• ไม่สามารถจัดการประเภทการลาได้<br>• ไม่สามารถลบใบลาที่อนุมัติหรือไม่อนุมัติแล้วได้ |
| **✅ หัวหน้างาน (Manager)** | `manager` | ทุกใบ | ได้ | **ได้** | **ไม่ได้** | **ไม่ได้** | • ไม่สามารถลบใบลาของใครได้ (ป้องกันการทำลายหลักฐานประวัติการลา)<br>• ไม่สามารถเพิ่ม แก้ไข หรือลบประเภทการลาได้ |
| **🧾 ฝ่ายบุคคล (HR)** | `hr` | ทุกใบ | ได้ | **ได้** | **ได้** | **ได้ (ทุกใบ)** | • ไม่มีข้อจำกัดด้านฟังก์ชันการทำงานในระบบ แต่ต้องผ่านการยืนยันตัวตนด้วยบัญชีที่ถูกต้อง |

---

## 2. การบังคับใช้สิทธิ์บนหน้าจอ (Client-side UI Controls)

ระบบ LeaveEasy มีกลไกการแสดงผลตามบทบาทของผู้ใช้ดังนี้:

1. **แถบเมนูนำทาง (`js/nav.js`):**
   - เมนู **"ประเภทการลา" (`leave-types.html`)** จะถูกซ่อนอัตโนมัติหากผู้ใช้ไม่ได้ถือบทบาท `hr`
   - หากผู้ใช้ที่ไม่มีสิทธิ์พยายามพิมพ์ URL ตรงเข้ามา จะถูกแจ้งเตือนและส่งกลับไปหน้ารายการใบลาทันที
   - แสดงชื่อผู้ใช้และป้ายกำกับบทบาท (`employee`, `manager`, `hr`) ที่มุมขวาบนอย่างชัดเจน

2. **หน้ารายการใบลา (`js/leave-requests.js`):**
   - ผู้ใช้บทบาท `employee` จะเห็นเฉพาะใบลาที่ตนเองเป็นผู้ยื่น (`requesterId == auth.currentUser.uid`)
   - ผู้ใช้บทบาท `manager` และ `hr` จะเห็นใบลาทั้งหมดในระบบ เพื่อความสะดวกในการติดตามงานของทีมและองค์กร

3. **หน้ารายละเอียดใบลา (`js/leave-request-detail.js`):**
   - **ปุ่ม "อนุมัติ" และ "ไม่อนุมัติ":** ซ่อนสำหรับผู้ใช้บทบาท `employee` และแสดงเฉพาะสำหรับ `manager` และ `hr` เมื่อสถานะเป็น `รอพิจารณา`
   - **ปุ่ม "ลบใบขอลานี้":**
     - สำหรับ `employee`: แสดงเฉพาะใบที่ตนเองยื่นและยังอยู่ในสถานะ `รอพิจารณา` เท่านั้น
     - สำหรับ `manager`: ซ่อนปุ่มลบ เพื่อคงความสมบูรณ์ของข้อมูล
     - สำหรับ `hr`: แสดงปุ่มลบสำหรับทุกใบ

---

## 3. ความปลอดภัยที่แท้จริง (Security Rules vs UI Hiding)

> ⚠️ **ข้อพึงระวังด้านความปลอดภัย:**  
> การซ่อนปุ่มบนหน้าเว็บ (UI-level hiding) เป็นเพียงการอำนวยความสะดวกและแนะนำประสบการณ์ใช้งานที่ดีแก่ผู้ใช้ (User Experience) เท่านั้น ผู้ใช้ที่มีความรู้ทางเทคนิคยังสามารถเปิด Developer Console หรือส่งคำขอผ่านเครือข่ายได้โดยตรง  
> ความปลอดภัยและการคุ้มครองข้อมูลที่แท้จริง ต้องถูกควบคุมด้วย **Firestore Security Rules** ที่ฝั่งเซิร์ฟเวอร์ (`firestore.rules`)
