rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // กฎขั้นต่ำตามใบงานที่ 2 (สัปดาห์ที่ 7): ผู้ใช้ต้องผ่านการยืนยันตัวตน (ล็อกอิน) ก่อน จึงจะอ่านหรือเขียนข้อมูลได้
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
